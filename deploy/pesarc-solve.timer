# Runs the solver on a schedule. Change OnCalendar to taste — e.g. *:0/5 for
# every 5 minutes (Vercel Hobby capped this at daily; a droplet has no such limit).
[Unit]
Description=Schedule the Pesarc settlement solver

[Timer]
OnCalendar=*:0/5
Persistent=true

[Install]
WantedBy=timers.target
