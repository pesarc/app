// Deprecated: the hero now uses the interactive <Globe /> component.
// Kept as a re-export so any stale imports keep working.
export { default, type GlobeControls as ParticleControls } from "./Globe";
