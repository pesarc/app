// Single source of truth for on-chain interfaces consumed by web / mobile /
// api / worker. Generated from the `contracts` repo — never hand-edit ABIs in
// app code again. Run `pnpm --filter @stablearc/abi sync` after a contracts build.
//
// EVM: `as const` ABI arrays (viem infers types from these).
// SVM: Anchor IDL JSON imported here.
export const predictionMarketAbi = [] as const; // populated by sync
export const realizedRateOracleAbi = [] as const; // populated by sync
export {}; // placeholder until first sync
